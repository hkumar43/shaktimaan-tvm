# shaktimaan-tvm
pytorchmobi
it contains the quantized mobilenetv2 pretrained onnx model(taken from docs)
a program to convert into relay model and then its compilation for generating .so and ,tar file
The input image is taken with preprocessing it as per the model input requirement in rgb format and normalization (reference from doc)
Then test the model using cat,fish,vulture image
